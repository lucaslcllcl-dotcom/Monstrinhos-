--- a/app/build.gradle
+++ b/app/build.gradle
@@ -7,7 +7,7 @@
         applicationId 'com.caeless.monstrinhos'
         minSdk 26
         targetSdk 36
-        versionCode 6880
-        versionName '6.8.8'
+        versionCode 6890
+        versionName '6.8.9'
     }
 }
--- a/app/src/main/AndroidManifest.xml
+++ b/app/src/main/AndroidManifest.xml
@@ -1,5 +1,5 @@
 <manifest xmlns:android="http://schemas.android.com/apk/res/android">
-    <application android:theme="@style/AppTheme" android:label="Monstrinhos TCC 6.8.8 IDIOMA ÚNICO" android:allowBackup="false" android:supportsRtl="true">
+    <application android:theme="@style/AppTheme" android:label="Monstrinhos TCC 6.8.9 IDIOMA TOQUE CORRIGIDO" android:allowBackup="false" android:supportsRtl="true">
         <activity android:name=".MainActivity" android:screenOrientation="portrait" android:exported="true">
             <intent-filter>
                 <action android:name="android.intent.action.MAIN"/><category android:name="android.intent.category.LAUNCHER"/>
--- a/app/src/main/java/com/caeless/monstrinhos/MonstrinhosView.java
+++ b/app/src/main/java/com/caeless/monstrinhos/MonstrinhosView.java
@@ -115,17 +115,19 @@
     }
     private void drawLanguageMenu(Canvas c,float w,float h){
         if(!languageMenuOpen)return;
-        // Seletor temático desenhado no mesmo Canvas: nenhuma View/Spinner/PopupWindow Android é criada.
-        float l=w*.555f,r=w*.895f,t=h*.565f,b=h*.635f,rad=h*.018f;
-        p.setShader(new LinearGradient(0,t,0,b,0xFFFFF6DF,0xFFFFDDA8,Shader.TileMode.CLAMP));
+        // v6.8.9: seletor horizontal dentro da PRÓPRIA faixa de Idioma.
+        // Não invade Acessibilidade nem Créditos e não cria View/Spinner/PopupWindow Android.
+        float l=w*.455f,r=w*.910f,t=h*.642f,b=h*.700f,rad=h*.014f;
+        p.setShader(new LinearGradient(0,t,0,b,0xFFFFF8E8,0xFFFFE4B8,Shader.TileMode.CLAMP));
         c.drawRoundRect(l,t,r,b,rad,rad,p);p.setShader(null);
-        p.setStyle(Paint.Style.STROKE);p.setStrokeWidth(2.4f);p.setColor(0xFFB45A35);c.drawRoundRect(l,t,r,b,rad,rad,p);p.setStyle(Paint.Style.FILL);
+        p.setStyle(Paint.Style.STROKE);p.setStrokeWidth(2.2f);p.setColor(0xFFB45A35);c.drawRoundRect(l,t,r,b,rad,rad,p);p.setStyle(Paint.Style.FILL);
         String[] names={"Português","English","Español"};
-        float row=(b-t)/3f;
+        float col=(r-l)/3f;
         for(int i=0;i<3;i++){
-            if(i==s.languageIndex){p.setColor(0x33E75A6A);c.drawRoundRect(l+4,t+i*row+2,r-4,t+(i+1)*row-2,rad*.45f,rad*.45f,p);}
-            textFit(c,names[i],(l+r)/2,t+(i+.69f)*row,14.5f,11.5f,(r-l)*.84f,0xFF6A351B);
-            if(i<2){p.setColor(0x55B45A35);c.drawRect(l+w*.025f,t+(i+1)*row,r-w*.025f,t+(i+1)*row+1,p);}
+            float cl=l+i*col,cr=cl+col;
+            if(i==s.languageIndex){p.setColor(0x33E75A6A);c.drawRoundRect(cl+3,t+3,cr-3,b-3,rad*.55f,rad*.55f,p);}
+            textFit(c,names[i],(cl+cr)/2,t+(b-t)*.64f,12.8f,9.8f,col*.90f,0xFF6A351B);
+            if(i<2){p.setColor(0x55B45A35);c.drawRect(cr,t+h*.008f,cr+1,b-h*.008f,p);}
         }
     }
     private void woodTextPatch(Canvas c,String title,String sub,float w,float h){
@@ -268,7 +270,7 @@
         p.setColor(0x42FFFFFF);c.drawRoundRect(l+w*.025f,top+h*.004f,r-w*.025f,top+(bot-top)*.28f,rad*.55f,rad*.55f,p);
         textFit(c,reaction,w*.50f,top+(bot-top)*.64f,17.5f,14f,(r-l)*.90f,0xFF65391E);
     }
-    private void drawVersion(Canvas c,float w,float h){text(c,"v6.8.8 UI",w*.91f,h*.988f,8,0xCCFFFFFF);}
+    private void drawVersion(Canvas c,float w,float h){text(c,"v6.8.9 UI",w*.91f,h*.988f,8,0xCCFFFFFF);}
 
     private void settingSlot(Canvas c,float l,float t,float r,float b,int c1,int c2){
         // Máscara opaca em degradê, combinando com a faixa original, para apagar completamente o switch/texto impresso.
@@ -405,7 +407,7 @@
         creamTag(c,tr("Projeto acadêmico • TCC","Academic project • TCC","Proyecto académico • TCC"),w*.22f,h*.335f,w*.78f,h*.385f);
         text(c,tr("Jogo infantil Android","Android children’s game","Juego infantil Android"),w*.50f,h*.445f,18,0xFF6A4A32);
         text(c,tr("Identidade visual: Monstrinhos","Visual identity: Monstrinhos","Identidad visual: Monstrinhos"),w*.50f,h*.495f,16,0xFF6A4A32);
-        text(c,tr("Versão funcional 6.8.8","Functional version 6.8.8","Versión funcional 6.8.8"),w*.50f,h*.545f,16,0xFF6A4A32);
+        text(c,tr("Versão funcional 6.8.9","Functional version 6.8.9","Versión funcional 6.8.9"),w*.50f,h*.545f,16,0xFF6A4A32);
         text(c,"com.caeless.monstrinhos",w*.50f,h*.595f,12,0xFF6A4A32);
         text(c,tr("Progresso salvo localmente no aparelho","Progress saved locally on the device","Progreso guardado localmente en el dispositivo"),w*.50f,h*.645f,14,0xFF6A4A32);
         glossyButton(c,"↩",tr("VOLTAR","BACK","VOLVER"),w*.24f,h*.715f,w*.76f,h*.785f,0xFF43B7FF,0xFF147FD7,0xFF0A5BA6);
@@ -628,20 +630,22 @@
                 if(y>h*.73f&&y<h*.86f){int theme=clamp((int)(x/(w*.25f)),0,3);react(s.selectOrBuyDecor(getContext(),theme)?tr("Tema: ","Theme: ","Tema: ")+localDecor():tr("Faltam moedas para esse tema.","Not enough coins for this theme.","Faltan monedas para este tema."));}
                 break;
             case 5:
-                if(x<w*.25f&&y<h*.13f){screen=0;break;}
-                if(y>h*.25f&&y<h*.35f){s.toggleSound(getContext());react(tr("Som ","Sound ","Sonido ")+(s.soundOn==1?onWord():offWord())+".");break;}
-                if(y>h*.35f&&y<h*.44f){s.toggleMusic(getContext());react(tr("Música ","Music ","Música ")+(s.musicOn==1?onWord():offWord())+".");break;}
-                if(y>h*.44f&&y<h*.53f){s.toggleVibration(getContext());react(tr("Vibração ","Vibration ","Vibración ")+(s.vibrationOn==1?onWord():offWord())+".");break;}
-                if(y>h*.53f&&y<h*.63f){screen=13;break;}
+                // v6.8.9: com o menu aberto, ele recebe PRIORIDADE ABSOLUTA sobre todos os botões de fundo.
                 if(languageMenuOpen){
-                    if(x>w*.555f&&x<w*.895f&&y>h*.565f&&y<h*.635f){
-                        int pick=clamp((int)((y-h*.565f)/(h*.070f/3f)),0,2);
+                    if(x>w*.455f&&x<w*.910f&&y>h*.642f&&y<h*.700f){
+                        float col=w*(.910f-.455f)/3f;
+                        int pick=clamp((int)((x-w*.455f)/col),0,2);
                         s.languageIndex=pick;s.save(getContext());languageMenuOpen=false;
                         setContentDescription(tr("Monstrinhos. Jogo infantil com fases, coleção, ovos e casinha.","Monstrinhos. Children’s game with stages, collection, eggs and house.","Monstrinhos. Juego infantil con fases, colección, huevos y casita."));
                         reaction="";reactionUntil=0L;invalidate();break;
                     }
-                    languageMenuOpen=false;invalidate();break;
+                    languageMenuOpen=false;reaction="";reactionUntil=0L;invalidate();break;
                 }
+                if(x<w*.25f&&y<h*.13f){screen=0;break;}
+                if(y>h*.25f&&y<h*.35f){s.toggleSound(getContext());react(tr("Som ","Sound ","Sonido ")+(s.soundOn==1?onWord():offWord())+".");break;}
+                if(y>h*.35f&&y<h*.44f){s.toggleMusic(getContext());react(tr("Música ","Music ","Música ")+(s.musicOn==1?onWord():offWord())+".");break;}
+                if(y>h*.44f&&y<h*.53f){s.toggleVibration(getContext());react(tr("Vibração ","Vibration ","Vibración ")+(s.vibrationOn==1?onWord():offWord())+".");break;}
+                if(y>h*.53f&&y<h*.63f){screen=13;break;}
                 if(y>h*.63f&&y<h*.72f){languageMenuOpen=true;reaction="";reactionUntil=0L;invalidate();break;}
                 if(y>h*.72f&&y<h*.82f){screen=14;break;}
                 break;
--- a/tools/verify_release.py
+++ b/tools/verify_release.py
@@ -6,8 +6,8 @@
 checks={
 'package':'com.caeless.monstrinhos' in text,
 'api36':'compileSdk 36' in text and 'targetSdk 36' in text,
-'version':'versionCode 6880' in text and "versionName '6.8.8'" in text,
-'label':'Monstrinhos TCC 6.8.8 IDIOMA ÚNICO' in text,
+'version':'versionCode 6890' in text and "versionName '6.8.9'" in text,
+'label':'Monstrinhos TCC 6.8.9 IDIOMA TOQUE CORRIGIDO' in text,
 'backup_disabled':'android:allowBackup="false"' in text,
 'save':'monstrinhos_save_v1' in text,
 'references':all(x in text for x in ['R.drawable.tcc_home','R.drawable.tcc_map','R.drawable.tcc_gameplay','R.drawable.tcc_album','R.drawable.tcc_house','R.drawable.tcc_settings','R.drawable.tcc_achievements','R.drawable.tcc_hatch']),
